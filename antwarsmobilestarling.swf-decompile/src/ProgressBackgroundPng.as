package
{
   import flash.display.Bitmap;

   // Stubbed Embed
   public class ProgressBackgroundPng extends Bitmap
   {
      public function ProgressBackgroundPng()
      {
         super();
      }
   }
}
